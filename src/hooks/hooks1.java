package hooks;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import io.cucumber.java.Before;

public class hooks1 {
			
	/*
	 * @Before("@Sanitytest") public void beforevalidation() { WebDriver driver =
	 * new FirefoxDriver(); driver.get("www.gmail.com"); }
	 */
	
}
